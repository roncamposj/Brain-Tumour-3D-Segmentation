# Brain-Tumour-3D-Segmentation

To train model, run:
```
python -m 3dseg.py
```

To observe results from, run ```load_and_plot.ipynb```. This file uses results from saved 5-fold cross validation run, saved in ```metrics.npz```.
